name: Release

on:
  push:
    branches:
      - master

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Release Please
        uses: release-please/action@v2.7.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}